{
  "xhs_cookie": {
    "description": "小红书网页版 Cookie（登录后从开发者工具复制）",
    "type": "string",
    "default": "",
    "hint": "登录 xiaohongshu.com 后 F12 → Application → Cookies → 复制整个 Cookie 字符串"
  },
  "dy_cookie": {
    "description": "抖音 Cookie（可选，手机版方案不需要也能搜索）",
    "type": "string",
    "default": "",
    "hint": "支持两种格式：① 常规 Cookie 字符串 ② EditThisCookie 等插件导出的 JSON 数组"
  },
  "max_results": {
    "description": "每个平台最多返回多少条结果",
    "type": "int",
    "default": 10
  },
  "similarity_threshold": {
    "description": "相似度阈值（0-100），高于此值视为重复",
    "type": "int",
    "default": 85
  },
  "headless": {
    "description": "是否无头模式（设为 false 可看到浏览器窗口）",
    "type": "bool",
    "default": true
  }
}